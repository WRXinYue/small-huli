<UserControl x:Class="Multiple_Views.Views.SideMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Rectangle Fill="#202020" Grid.RowSpan="2" />
        <!--<ListBox Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" ItemContainerStyle="{StaticResource SplitNavigationListBox}">
            <ListBoxItem Content="一键启动" />
            <ListBoxItem Content="高级选项" />
            <ListBoxItem Content="疑难解答" />
            <ListBoxItem Content="模型管理" />
            <ListBoxItem Content="小工具" />
            <ListBoxItem Content="设置" />
        </ListBox>-->
    </Grid>
</UserControl>
